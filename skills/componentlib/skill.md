# ComponentLib Skill

Component libraries. **Triggers:** `ComponentLib`, `design system`, `UI library`

**Structure:** `components/Button/{Button.tsx, Button.test.tsx, Button.stories.tsx}` + `tokens/{colors, typography, spacing}`

**Pattern:** CVA variants (variant, size) + VariantProps + Storybook docs
