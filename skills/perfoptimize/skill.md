# PerfOptimize Skill

Performance optimization. **Triggers:** `PerfOptimize`, `optimize performance`, `speed up`

**Vitals:** LCP <2.5s | FID <100ms | CLS <0.1 | TTFB <800ms

**Bundle:** `vite-bundle-visualizer`, code splitting (React.lazy), tree shaking, gzip/Brotli

**React:** React.memo, useMemo, useCallback, @tanstack/react-virtual, Suspense

**Images:** WebP/AVIF, srcset, loading="lazy", CDN

**Database:** Eager loading, indexes, pagination, Redis cache
