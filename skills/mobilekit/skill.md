# MobileKit Skill

React Native/Expo. **Triggers:** `MobileKit`, `React Native`, `Expo setup`

**Setup:** `npx create-expo-app@latest --template tabs`

**Essential Libraries:** Navigation (@react-navigation), State (zustand), Forms (react-hook-form), API (@tanstack/react-query), UI (tamagui), Animations (reanimated)

**Performance:** FlashList, expo-image, memoization, Hermes, useNativeDriver

**Deploy:** EAS Build, EAS Update, EAS Submit
