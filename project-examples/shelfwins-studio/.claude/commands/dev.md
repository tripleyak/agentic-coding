# Start Development Server

Start the Vite development server for ShelfWins Studio.

## Instructions

1. Run `npm run dev` to start the development server
2. Report the local URL (usually http://localhost:3000)
3. Confirm the server is running successfully
4. If there are any startup errors, report them clearly

Do NOT open a browser - just start the server and report the URL.
